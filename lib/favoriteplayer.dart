import 'package:flutter/material.dart';

class favoriteplayer extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
        child: Text("お気に入りの選手")
    );
  }
}